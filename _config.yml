title: GoodsLab
tagline: Honest Unboxing Reviews & Smart Storage Picks
description: >-
  GoodsLab covers real-world unboxing reviews and home storage recommendations,
  built from hands-on testing so you can buy smarter and skip the duds.
# After connecting your Cloudflare domain, change the url below to your real domain, e.g. "https://www.example.com"
url: ""
baseurl: ""
lang: en
locale: en_US

author:
  name: GoodsLab Editorial Team

# Categories are plain strings, Jekyll auto-indexes them under site.categories
markdown: kramdown
kramdown:
  input: GFM
permalink: /articles/:categories/:title/

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: post
  - scope:
      path: ""
      type: "pages"
    values:
      layout: page

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
  - node_modules
