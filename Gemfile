source "https://rubygems.org"

# 使用 github-pages gem 可確保本機版本跟 GitHub Pages 實際部署環境一致
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

# Windows 使用者需要這個
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]
